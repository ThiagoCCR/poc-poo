export class Product {
    public name: string;
    public productClass: string;
  
    constructor(
      name: string,
      productClass: string
    ) {
      this.name = name;
      this.productClass = productClass;
    }
}